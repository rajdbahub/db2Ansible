 - name: OS and DB2 commands
   hosts: rajoo-ubuntu
   tasks:
   - name: OS version display
     shell: uname -v
     register: var
   - debug: msg="{{var.stdout}}"

   - name: change directory
     shell: cd /opt/ibm/db2/V11.1;pwd
     register: db2loc
   - debug: msg="{{db2loc.stdout}}"
